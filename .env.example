HTTP_ADDR=:8080
DATABASE_URL=postgres://face_control:face_control@localhost:5432/face_control?sslmode=disable

MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=face-photos
MINIO_USE_SSL=false

# HASDK_MODEL_DIR=/opt/hasdk/model     # only needed when building with -tags hasdk
